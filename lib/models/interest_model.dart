import 'package:flutter/material.dart';

class InterestModel {
  final String? title;
  final bool? isSelected;
  final int id;

  InterestModel( {this.title, this.isSelected=false, required this.id,});
}
