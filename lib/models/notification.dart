import 'package:flutter/material.dart';

class NotificationModel {
  final String? day;
  final String? mount;
  final String? title;
  final bool? isRead;

  NotificationModel({this.day, this.mount, this.title, this.isRead});
}
